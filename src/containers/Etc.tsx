import React from 'react';
import { Outlet } from 'react-router';

/**
 * @description 기타 Container (path: "etc")
 */

const Etc = () => {
    return (
        <>
            <Outlet/>
        </>
    );
};

export default Etc;